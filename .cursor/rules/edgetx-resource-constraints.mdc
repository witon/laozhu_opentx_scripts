---
description: EdgeTX/Lua 运行环境性能与内存受限时的实现原则：简洁优先、少兜底、封装为复用、控制开销。
alwaysApply: true
---

# 遥控器 Lua：性能与内存约束

本仓库脚本运行在 EdgeTX 遥控器上，**CPU 与 Lua 堆内存均有限**。

编写或修改代码时：

- **实现尽量简洁**：能直写就不绕弯；避免仅为「好看」或「通用」而膨胀代码路径。
- **非必要不保护、不兜底**：不为理论上不可能、或产品未要求覆盖的边缘情况层层判空、try/兜底；确有必要时再补（例如外设 I/O、用户可破坏的不变量）。
- **封装主要为复用**：抽函数/模块是为了多处共用或清晰边界，而不是为单点调用造抽象层。
- **控制性能与内存**：少临时表/闭包/大字符串拼接；注意按需加载与释放（与本项目 uilib 等现有约定一致）；避免在热路径里做冗余分配或重复工作。

与 `karpathy-guidelines.mdc` 的「简单优先」一致；在**资源约束**场景下，**省略非必要防御性代码**是预期风格，除非任务或日志/安全规范明确要求。
